info:
  name: Get unknown recipe
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8000/recipes/999999
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: 404

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
